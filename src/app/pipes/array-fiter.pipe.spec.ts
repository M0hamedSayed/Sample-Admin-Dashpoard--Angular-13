import { ArrayFiterPipe } from './array-fiter.pipe';

describe('ArrayFiterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayFiterPipe();
    expect(pipe).toBeTruthy();
  });
});
